import React from 'react';
import { StyleSheet, Text } from 'react-native';

const ValidAddressScreen = () => {
    return <Text>This is an Address Screen</Text>;
};

const styles = StyleSheet.create({})

export default ValidAddressScreen
